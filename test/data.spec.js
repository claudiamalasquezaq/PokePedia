require('../src/data.js');


describe('pokemon', () => {
  it('debería ser un objeto', () => {
    expect(typeof pokemon).toBe('object');
  });

  // it('returns `example`', () => {
  //   expect(example()).toBe('example');
  // });
});
